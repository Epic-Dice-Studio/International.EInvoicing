<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>International.EInvoicing.Core</PackageId>
    <Description>
      Core building blocks of International.EInvoicing: the EN 16931 canonical invoice model, the raw-preserving
      Field&lt;T&gt; value system, the diagnostic engine, the format/country registry and the hardened XML plumbing.
    </Description>
    <PackageTags>e-invoicing;einvoice;en16931;ubl;cii;factur-x;zugferd;xrechnung;peppol;invoice</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Options" />
  </ItemGroup>

</Project>
